// @waypoint/core — domain: hierarchy, ask lifecycle, computed `blocked` +
// `blast_radius`, optimistic concurrency. Depends ONLY on @waypoint/shared and
// talks to persistence through repository ports. Populated in `add-core-ask-loop`.
export {};
