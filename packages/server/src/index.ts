// @waypoint/server — adapters: MCP (Streamable HTTP), REST, WebSocket; Postgres
// repository implementing core's ports; event emitter + cache. Depends on
// @waypoint/core + @waypoint/shared. Populated in `add-core-ask-loop`.
export {};
